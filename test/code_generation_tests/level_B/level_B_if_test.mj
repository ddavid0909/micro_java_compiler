program test302

int i;
bool ibool;

int niz2;

int niz[];
int a;

{
	int sum(int z, int d){
		return z + d;
	}	

	int inkrement(int a, int inc) 
		int pom; 
	{
		if(inc == 1) pom = 1;
		else if(inc == 2) pom = a;

		return sum(a,pom);
	}
	
	bool verify() {
		print('v');
		return false;
	}

	int f1(int arg){
		print(arg); print(eol);
		return arg * 2;
	}
	
	void main()
	{
		
		if(1!=1) print(-6);
		else print(0);
		print(eol); //0
			
		ibool = false;
		i = 5;
		if(ibool)
			if(i == 5) print(1); //1
			else print(-1); 
		
		a = 2;	
		niz = new int[3];
		niz[0] = 1 * a;
		niz[1] = 2 * a;
		niz[2] = sum(niz[1], niz[0]) * a + 1;
		if(verify() || niz[2] == 4) print(ord('A')); 
		else print(ord('a')); //a
		print(eol);
	
		}
}